node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri
src-tauri/target
gen/schemas

# Bundled Node runtime — fetched by `npm run fetch:node`, not committed
# (100MB+ binary; every dev/build pulls the pinned version fresh)
src-tauri/resources/node
.tmp-node-fetch
